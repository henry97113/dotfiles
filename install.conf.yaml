- defaults:
    link:
      relink: true

- clean: ["~"]
- link:
    ~/.zshrc: zshrc
# - create:
#     - ~/downloads
#     - ~/.vim/undo-history
# - shell:
#     - [git submodule update --init --recursive, Installing submodules]
