- defaults:
    link:
      relink: true

- clean: ["~"]

- link:
    ~/.zshrc:
    ~/.gitconfig:
    ~/.hushlogin:

# - shell:
#     - [git submodule update --init --recursive, Installing submodules]
- create:
    - ~/Work
